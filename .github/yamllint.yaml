---
extends: default

rules:
  line-length:
    max: 150
    level: warning
  comments-indentation: disable
  document-start: disable
  truthy:
    allowed-values: ['true', 'false']
    check-keys: false

ignore: |
  .git/
